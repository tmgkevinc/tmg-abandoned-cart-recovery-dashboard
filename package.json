{
  "name": "tmg-abandoned-cart-recovery-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "TMG abandoned cart lead recovery dashboard backed by the Data Hub API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check src/index.js && node scripts/verify-dashboard-identity.js",
    "verify:identity": "node scripts/verify-dashboard-identity.js",
    "deploy:cloudflare": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "latest"
  }
}
