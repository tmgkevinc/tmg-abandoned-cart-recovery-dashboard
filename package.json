{
  "name": "tmg-high-shipping-no-completed-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "TMG high shipping not completed draft dashboard backed by the Data Hub API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check src/index.js && node scripts/verify-dashboard-identity.js",
    "verify:identity": "node scripts/verify-dashboard-identity.js",
    "deploy:cloudflare": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "latest"
  }
}
